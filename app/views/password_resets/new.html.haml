= form_tag password_resets_path, :method => :post do
  .field
    = label_tag t('sessions.new.email')
    = text_field_tag :email, params[:email]
  .actions
    = submit_tag t('sessions.new.reset_password')
