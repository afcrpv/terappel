%li= link_to "#{t('sessions.greet')} #{current_user.username} (#{current_user.role}, #{current_user.centre_name})", edit_user_path(current_user) if can? :edit, current_user
%li
  = link_to logout_path do
    %span.label.label-important= t('sessions.logout')
