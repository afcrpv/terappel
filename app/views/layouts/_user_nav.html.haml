%li
  .navbar-text
    #{t('sessions.greet')}
    %strong #{current_user.username}
    (#{current_user.roles.first}, #{current_user.centre_name}) !
%li
  = link_to logout_path do
    %span.label.label-important= t('sessions.logout')
