!!!

%html
  %head
    %meta{"http-equiv"=>"Content-Type", :content =>"text/html; charset=utf-8"}/
  %body
    %h1
      #{t('sessions.password_resets.mail_hello')} #{@user.email}
      %p= t('sessions.password_resets.mail_header')
      %p= @url
      %p= t('sessions.password_resets.mail_footer')
