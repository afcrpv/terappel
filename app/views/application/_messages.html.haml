- flash.each do |name, msg|
  - if msg.is_a?(String)
    .alert.fade.in{class: "alert-#{name == 'notice' ? 'success' : 'danger'}"}
      %a.close{'data-dismiss': 'alert', 'aria-hidden': true, href: '#'} &times;
      = content_tag :div, msg, id: "flash_#{name}"
